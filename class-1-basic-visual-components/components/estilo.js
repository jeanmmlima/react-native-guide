import { Dimensions, StyleSheet } from 'react-native'

export default StyleSheet.create({
    txtG: {
        fontSize: 32,
        textAlign: "center",
    },

    textInput: {
        fontSize: 30,
        width: '50%',
        borderBottomWidth: 1,
        textAlign: 'center',
        justifyContent: 'center',
        margin: 20,
        padding: 10
    }
})